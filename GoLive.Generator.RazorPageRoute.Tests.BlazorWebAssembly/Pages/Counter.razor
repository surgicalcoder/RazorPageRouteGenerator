@page "/counter"
@page "/counter/view/{id}"
@page "/counter/viewbyid/{id:int}"

<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@code {
    private int currentCount = 0;

    [SupplyParameterFromQuery]
    public string QSInput { get; set; }
    
    private void IncrementCount()
    {
        currentCount++;
    }
    
    [JSInvokable("GoLive.Generator.RazorPageRoute.Tests.BlazorWebAsssembly.Pages.Counter.Test")]
    public void Test()
    {
        
    }
}
